@page "/"

<PageTitle>Index</PageTitle>

<br /><br />
<ProgressBar Title="Overall" Height="40px" Width=" " InitialValue="1" DisplayStringPercent="2"></ProgressBar>


@code {
    public static decimal NickMonthlyIncome = 4758.38M;
    public static decimal LilianMonthlyIncome = 3066.00M;

    public static decimal TotalMonthlyIncome = Math.Round(NickMonthlyIncome + LilianMonthlyIncome, 2);
    //public static decimal TotalExpenses = Math.Round(NickExpenses.AllCosts.Sum() + LilianExpenses.AllCosts.Sum(), 2);

   // public static string TotalNet = Math.Round((TotalExpenses / TotalMonthlyIncome) * 100, 2) + "%";
   // public static string DisplayGross = $"${TotalExpenses}/${TotalMonthlyIncome} --> {Math.Round((TotalExpenses / TotalMonthlyIncome) * 100, 2)}%";

    public class ExpenseItem
    {
        public string Description { get; set; } = string.Empty;
        public decimal Amount { get; set; }
        public int Id { get; set; }
    }

    public class ExpenseItemVariable
    {
        public int Id { get; set; }
        public DateTime TransactionDate { get; set; }
        public decimal Amount { get; set; }
        public string Description { get; set; } = string.Empty;
    }
}